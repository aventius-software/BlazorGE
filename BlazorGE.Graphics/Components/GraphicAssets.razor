@inherits GraphicAssetsBase

@foreach (var asset in Assets)
{
    <img hidden src="@asset.Url" @ref="@asset.ImageElementReference" @onload="@(() => OnLoadGraphicAssetAsync(asset.UniqueIdentifier))" />
}